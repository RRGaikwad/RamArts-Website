# Firebase configuration — copy to .env and fill with your project values
# Get these from Firebase Console → Project Settings → Your apps → Web app

VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789012
VITE_FIREBASE_APP_ID=1:123456789012:web:abcdef123456

# Comma-separated admin UIDs (must match firestore.rules allowlist)
# After creating the admin user in Firebase Auth, copy their UID here and into firestore.rules / storage.rules
VITE_ADMIN_UIDS=your_admin_uid_here
